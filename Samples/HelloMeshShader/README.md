# Immortal Graphics Hello Mesh Shader Example

You can change the backendAPI to BackendAPI::D3D12 to use Direct3D 12 as backend instead of Vulkan.
```C++
int main(int, char **)
{
    BackendAPI backendAPI = BackendAPI::Vulkan;
}
```
